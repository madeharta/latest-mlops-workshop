# Requirements utama untuk development lokal Sesi 1-8 (via venv atau Dev Container).
# Untuk image Docker (Sesi 6), gunakan api/requirements.txt yang lebih ramping.
#
# Versi di bawah adalah versi PERSIS yang sudah diuji berjalan bersama-sama
# untuk seluruh kode di materi ini. Catatan penting -- lihat README untuk
# penjelasan lengkap tiap baris yang ditandai (*):

pyyaml==6.0.3
pandas==3.0.2
scikit-learn==1.8.0
joblib==1.5.3
pytest==8.3.4

numpy>=1.26                    # (*) evidently 0.7.21 tanpa batas atas -- lihat README -- Sesi 7
mlflow==3.16.0                  # (*) tracking backend file:// deprecated -- pakai sqlite:///, lihat Sesi 4

fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
requests==2.33.1

dvc[gs]==3.67.1

evidently==0.7.21               # (*) API lama (column_mapping) tetap dipakai via evidently.legacy -- lihat README -- Sesi 7
plotly==5.24.1                   # evidently 0.7.21 sudah deklarasikan plotly<6 sendiri -- pin ini untuk reproducibility, bukan wajib

prefect==3.8.6                   # Sesi 8 -- orkestrasi
